enum SortType{
    RATING,
    POPULAR, #etc
}

type Query{
    getUser(name: String) : User # Returns a User without Authentication for profile view
    getWhiskey(id: ID!) : Whiskey
    getReview(id: ID!) : Rating

    getWhiskeys(sortType : SortType): [Whiskey]
}

type Mutation{
    createUser(register : RegisterInput) : User
    editUser(register : RegisterInput) : User   #Verify user using JWT token

    createWhiskey(whiskey : WhiskeyInput) : Whiskey
    editWhiskey(whiskey : WhiskeyInput) : Whiskey

    createThumb(ratingId: ID!, thumb: Boolean) : Thumb #User: ID! identified by JWT
    editThumb(thumbId: ID!, thumb: Boolean) : Thumb

    createRating(rating : RatingInput) : Rating
    editRating(rating : RatingInput) : Rating

    login(auth : AuthInput) : User #TODO add session token in args?
    logout(auth : AuthInput) : String
}

# Input forms
input RatingInput{
    userId: ID!,
    whiskeyId: ID!,
}

input AuthInput{
    username: String!,
    password: String!
}

input RegisterInput{
    name: String,
    email: String,
    password: String,
    img: String,
}

input WhiskeyInput{
    title: String
    summary: String
    img: String

    price: Float
    volume: Float
    percentage: Float
}

# Type definitions
type User{
    id: ID!
    name: String
    img: String
    createdAt: String

    ratings: [Rating]
}

type Whiskey{
    id: ID!

    title: String
    summary: String
    img: String

    price: Float
    volume: Float
    percentage: Float

    ratings: [Rating]
    rating: Float # Average of all ratings
}

# Rating -> Review?
# Thumb -> Feedback/Vote?
type Rating{
    id: ID!
    user: User!
    whiskey: Whiskey!

    title: String
    body: String
    rating: Float
    createdAt: String

    thumbs: [Thumb]
}


type Thumb{
    id: ID!
    rating: Rating!
    user: User!

    thumb: Boolean
}